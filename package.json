{
  "name": "edgoing-website-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd website && npm run vercel:build",
    "start": "cd website && npm start",
    "dev": "cd website && npm run dev"
  },
  "workspaces": [
    "website"
  ]
}
